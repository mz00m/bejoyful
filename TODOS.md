# TODOs

## Design System

- [ ] Run `/design-consultation` to create DESIGN.md for bejoyful projects
  - **Why:** No documented design system exists. CSS custom properties and component patterns in kaleidoscope.html serve as a de facto system, but nothing prevents drift as features are added.
  - **What:** Document Space Grotesk typography, dark UI tokens (--bg, --surface, --border, --accent, --text, --text-dim), component patterns (slider-row, button, share-btn, drop-zone), and the new components added by the kaleidoscope animation feature (mood dropdown, progress ring, waveform bar, export popover).
  - **Depends on:** Kaleidoscope animation feature shipping first (so all new components exist to document).
